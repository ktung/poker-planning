pre-commit:
  parallel: true
  commands:
    format:
      run: pnpx prettier --write .
      stage_fixed: true
    lintfix:
      glob: '*.{js,ts,svelte}'
      run: pnpx eslint --fix {staged_files}
      stage_fixed: true
